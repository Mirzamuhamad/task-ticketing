{
  "name": "portal-task-ticketing",
  "private": true,
  "version": "1.0.0",
  "description": "Portal Task Ticketing System with NestJS, React, Socket.IO, and MySQL",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run start:dev -w server\" \"npm run dev -w client\"",
    "dev:server": "npm run start:dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w server && npm run build -w client",
    "lint": "npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
